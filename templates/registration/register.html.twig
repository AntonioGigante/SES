{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    <h1>Register</h1>

    {{ form_start(registrationForm) }}
    <div id="register" class="form-group row">
    <label class="col-sm-2 col-form-label">{{ form_row(registrationForm.username) }}</label>
    </div>
    <div class="form-group row">
    <label class="col-sm-2 col-form-label">{{ form_row(registrationForm.email) }}</label>
    </div>
    <div class="form-group row">
    <label class="col-sm-2 col-form-label">{{ form_row(registrationForm.plainPassword.first) }}</labeL>
    </div>
    <div class="form-group row">
    <label class="col-sm-2 col-form-label">{{ form_row(registrationForm.plainPassword.second) }}</label>
    </div>
    {{ form_end(registrationForm) }}
{% endblock %}
