{% extends 'base.html.twig' %}

{% block title %}Campeonatos{% endblock %}

{% block body %}
    <h1>Campeonatos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campeonato.id }}</td>
            </tr>
            <tr>
                <th>Campeonato</th>
               <td>{{ campeonato.campeonatoNombre }}</td>
            </tr>
            <tr>
                <th>Juego</th>
                <td>{{ campeonato.juego }}</td>
            </tr>
            <tr>
                <th>Pruebas</th>
                {#<td>{{ campeonato.pruebas }}</td>#}
            </tr>
            <tr>
                <th>Participantes</th>
                {% if participaciones %}
                    {% for participantes in participaciones %}
                    {#{% if participaciones.user is defined and participaciones.user is not null%}
                    {% for object campeonato %}
                        {% for campeonato in object.participantes %}#}
                            <ul>{{ participaciones.user }}</ul>
                        {#{% endfor %}
                        {% endfor %}
                    {% endif %}#}
                    {% endfor %}
                {% endif %}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('campeonatos_index') }}">back to list</a>

    <a href="{{ path('campeonatos_edit', {'id': campeonato.id}) }}">edit</a>

    {{ include('campeonatos/_delete_form.html.twig') }}
{% endblock %}
