{% extends 'base.html.twig' %}

{% block title %}Equipo{% endblock %}

{% block body %}
<table class="table table-hover">
    <h4>Equipo</h4>
 <thead>
    <tr class="table-info">
      <th scope="col">id</th>
      <th scope="col">Nombne</th>
      <th scope="col">Director</th>
      <th scome="col">Miembros</th>
      <th scome="col">Competiciones Inscrito</th>
      <th scope="col">Competiciones Ganadas</th>
      <th scope="col">Partidos disputados</th>
    </tr>
  </thead>
   <tr>
    {% if equipos  %}
        {% for equipo in equipos %}
        <tbody>
            <tr>
                <td>{{equipo.id}}</td>
                <td>{{equipo.nombre}}</td>
                <td>{{equipo.director}}</td>
                {#<td>{{equipo.miembros ? equipo.miembros|join(', ') : ''}}</td>
                <td>{{equipo.competicionesapuntadas}}</td>
                <td>{{equipo.competicionesganadas}}</td>
                <td>{{equipo.partidos}}</td>#}
            </tr>
            <p>falta añadir el resto de campos a la tabla de (Equipos)</p>
        </tbody>
        {% endfor %}
    {% endif %}
    </tr>
    </table>

    <a class="btn btn-lg btn-primary" type="submit"  href="{{ path('equipo') }}">back to list</a>

    {#<a href="{{ path('equipo_index') }}">back to list</a>

    <a href="{{ path('equipo_edit', {'id': equipo.id}) }}">edit</a>}

    {{ include('equipo/_delete_form.html.twig') }}#}
{% endblock %}
